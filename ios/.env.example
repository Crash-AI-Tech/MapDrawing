# A fresh checkout defaults to the committed staging endpoint.
EXPO_PUBLIC_API_ENV=staging

# Optional overrides for a LAN Worker or a different map style.
# EXPO_PUBLIC_API_BASE_URL=http://192.168.1.10:8787
EXPO_PUBLIC_MAP_STYLE_URL=https://tiles.openfreemap.org/styles/liberty
