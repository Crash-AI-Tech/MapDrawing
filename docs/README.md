# Documentation index

## Current documentation

- `architecture/`: current system and runtime architecture
- `product/`: current product and discoverability guidance
- `operations/`: release and operational procedures
- `releases/`: completed release records

## Archive

`archive/` contains superseded designs and one-time migration plans. It is retained for engineering history and must not be treated as current implementation guidance.

The source of truth for the current runtime is:

- `server/packages/contracts/src/`
- `server/db/migrations/`
- `server/cloudflare/wrangler.toml`
- `ios/lib/config.ts`
