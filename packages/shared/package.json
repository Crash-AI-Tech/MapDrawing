{
    "name": "@niubi/shared",
    "version": "1.0.0",
    "private": true,
    "main": "src/index.ts",
    "types": "src/index.ts",
    "scripts": {
        "build": "tsc",
        "type-check": "tsc --noEmit",
        "test": "pnpm build && node --test tests/*.test.mjs"
    },
    "devDependencies": {
        "typescript": "^5.7.0"
    }
}
